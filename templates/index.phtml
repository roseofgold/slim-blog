<?php include __DIR__ . '/header.phtml'; ?>
    <div class="container">
        <div class="entry-list">
        <?php
            if (empty($posts)) {
                echo 'Sorry, There are no posts yet';
            } else {
                foreach ($posts as $post) {
                    echo '<article>';
                    echo "<h2><a href=\"/blog/" . $post['id'] . "\">" . $post['title'] . "</a></h2>";
                    echo "<time datetime=\"" . $post['date'] . "\">" . date('F j, Y \a\t H:i',strtotime($post['date'])) . '</time>';
                    echo '</article>';
                }
            }
        ?>
        </div>
    </div>
<?php include __DIR__ . '/footer.phtml'; ?>