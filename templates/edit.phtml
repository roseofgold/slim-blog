<?php include __DIR__ . '/header.phtml'; ?>
    <div class="container">
        <div class="edit-entry">
            <h2>Edit Entry</h2>
            <form method="POST" action="">
                <label for="title">Title</label>
                <input type="text" name="title" value="<?= $posts[0]['title']; ?>" /><br>
                <label for="body">Entry</label>
                <textarea rows="5" name="body"><?= $posts[0]['body']; ?></textarea>
                <input type="submit" value="Save Entry" class="button">
                <a href="#" class="button button-secondary">Cancel</a>
                <input type="hidden" name="id" value="<?= $posts[0]['id']; ?>" />
                <?php foreach ($csrf as $key=>$value){
                    echo "<input type=\"hidden\" name=\"$key\" value=\"$value\">";
                } ?>
            </form>
        </div>
    </div>
<?php include __DIR__ . '/footer.phtml'; ?>